# =============================================================
#  PorxPy .gitignore
# =============================================================

# ----- Personal directories (not for repo) -------------------
user_*/

# Superseded resource files and sample CSVs kept for manual testing.
# Both are working material, not source: the archive is what the
# resource files used to be, and the test files exist to exercise the
# breakdown-upload flow by hand.
ZZ-archived/
test files/

# ----- Pre-loaded fund bundle --------------------------------
# The shipped fund set is distributed as a GitHub Release asset, never
# as a tracked file. It is a ~30 MB zip, and git cannot three-way-merge
# a zip: a tracked copy broke `git pull` for anyone who had re-exported
# over theirs, and every re-export added another 30 MB to the history of
# every clone, for ever. The folder itself stays (see its README) as the
# place to put the download; the zips never enter a commit.
PreLoadedFunds/*.zip

# ----- PorxPy runtime data (never commit user data) ----------
portfolios.json
settings.json
overrides.json
isin_map.json

# Cache directories (auto-rebuilt at runtime)
cache/

# Uploaded holdings files (user data, auto-managed by the app)
uploads/

# Resource CSVs are source-controlled and should be committed.
# Only exclude runtime-written JSON that the app may drop in resources/
resources/*.json

# ----- Python ---------------------------------------------
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/
.env

# ----- PyCharm / JetBrains --------------------------------
.idea/
*.iml
*.iws
*.ipr

# ----- VS Code --------------------------------------------
.vscode/

# ----- OS cruft -------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# ----- Misc -----------------------------------------------
*.stackdump
*.log
*.bak
*.swp
*.tmp
